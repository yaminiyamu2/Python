Keywords:
----------
    Keywords are predefined, reserved words used in Python programming.We cannot use a keyword as a variable name, function name,
or any other identifier.
List of Keywords:
None              await            class              while            if
break             else             finally            async            del
except            import           is                 elif             or
in                pass             return             yield            lambda
raise             and              as                 global           True
False             continue         def                assert           nonlocal
for               try              from               with             not

Identifiers:
------------
Identifiers are the name given to variables, classes, methods, etc.....
1.Variable Identifiers:
     ex: age = 25
         name = "Jhon"
         count = 0
         total_price = 100.50
         is_student = True
2.Method/Function Identifiers:
    ex: def calculate_area(length,width):
            return length * width
        def prient_greeting(name):
            prient("Hello, " + name + "!")
3.Class Identifiers:
    ex: class Student:
            def _ _init_ _(self ,name, age):
                  self.name = name
                  self.age = age
        class Calculator:
            def add(self, a, b):
                return a + b
Rules:
1.Start with a Letter or Underscore
2.Followed by Letters, Digits, or Underscores
3.Avoid Starting with a Digit
4.No Special Characters(!, @, $, etc.)
5.Case-Sensitive (e.g: age, Age, and AGE)
5.Avoid Using Keywords

Variables:
-----------
    In programming, a variable is a container (storage area) to hold data.
 EX: int x = 22
     string my_str = "hi"
     float y = 2.275
Difference between Variable and Identifiers:
---------------------------------------------
        Identifiers are names given to entities like variables, functions, etc., while variables are a specific type of 
identifier used to store and manipulate data in a program.

Constants:
----------
    A constant is a special type of variable whose value cannot be changed.
ex: # import constant file we created above
    import costant
    print(constant.PI) #prints 3.14 3.14
    print(constant.GRAVITY) #Prints 9.8

Literals:
---------
    Literals are representations of fixed values in a program. They can be numbers, characters, or strings, etc.
Types of Literals:
1. Numeric Literals
2. String Literals
3. Boolean Literals
4. None Literal
5. Bytes and Bytearray Literals
6. Raw String Literals
7. Numeric Separator
8. Collection Literals: List, Tuple, Set, Dictionary




